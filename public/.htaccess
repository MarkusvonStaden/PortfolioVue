RewriteEngine On

# Wenn Datei oder Ordner existiert, nichts tun
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Alles andere auf Root weiterleiten
RewriteRule ^ / [L]